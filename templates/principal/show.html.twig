{% extends 'base.html.twig' %}

{% block title %}Principal{% endblock %}

{% block body %}
    <h1>Principal</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ principal.id }}</td>
            </tr>
            <tr>
                <th>Blog</th>
                <td>{{ principal.Blog }}</td>
            </tr>
            <tr>
                <th>Usuario</th>
                <td>{{ principal.Usuario }}</td>
            </tr>
            <tr>
                <th>Fecha</th>
                <td>{{ principal.fecha ? principal.fecha|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('principal_index') }}">back to list</a>

    <a href="{{ path('principal_edit', {'id': principal.id}) }}">edit</a>

    {{ include('principal/_delete_form.html.twig') }}
{% endblock %}
